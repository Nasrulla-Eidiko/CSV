

CREATE FILTER MODULE CSV_TO_XML_Filter
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
		IF Root.DFDL.CSV_INPUTMSG.record.COMPANY = 'EIDIKO' THEN
			RETURN TRUE;
		ELSEIF Root.DFDL.CSV_INPUTMSG.record.COMPANY = 'COMPANY' THEN
			RETURN FALSE;
		END IF;
	END;

END MODULE;
